export function createPlayerData(
  minutes,
  points,
  field_goal_percentage,
  rebounds,
  assists,
  steals,
  blocks,
  turnovers,
) {
  return {
    minutes,
    points,
    field_goal_percentage,
    rebounds,
    assists,
    steals,
    blocks,
    turnovers,
  };
}
